/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
char* tree2str(struct TreeNode* root) {
    //It is not logical to do it in C
    //Due to its limitations on string operations
    //I have tried it but it is too hard to do it so I left trying it to do
    //In the next times, If I will find a easier solution for this question...
    //...I will try to implement it
}